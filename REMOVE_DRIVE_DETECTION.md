# Remove Drive Detection / Configure for Your Machine

**This guide has been consolidated.** Path and config setup (including removing hardcoded C:/G: drive logic and usernames) is now in:

- **[docs/CONFIGURATION.md](docs/CONFIGURATION.md)**

The current codebase uses **python-modules/config_reader.py** for path resolution (not a separate `common.py`). Follow CONFIGURATION.md for step-by-step changes.
